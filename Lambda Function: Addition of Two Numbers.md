# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```python
a=int(input())
b=int(input())
f = lambda a, b: a + b
result = f(a,b)
print(result)
```

## Output
<img width="1764" height="554" alt="Screenshot 2025-11-26 194534" src="https://github.com/user-attachments/assets/a92dd8a7-c1fa-4dcd-a74e-c6d3dfb859b8" />

## Result
Thus the python program to return sum of 2 arguments using Lambda function was executed and verified successfully .
